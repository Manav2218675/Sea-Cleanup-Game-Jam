using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectInfo : MonoBehaviour
{
    //set a check to only take the right object, check if object is in the correct slot or is logistic hub, 
    //check that both objects are the correct colour or are logistic hubs
    public bool connectable = false;
}
